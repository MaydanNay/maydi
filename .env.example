# Host port for nginx
WEB_PORT=8080

# Comma-separated browser origins (cookie/CORS). Local docker + prod:
CORS_ORIGINS=http://localhost:8080,https://maydi.net

# Must stay false on plain HTTP; true only behind HTTPS
SECURE_COOKIES=false
